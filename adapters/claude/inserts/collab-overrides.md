The harness's "use agents proactively" guidance applies in full to read-only subagents. For write-capable subagents and any `Workflow`, GATE-DELEGATE comes first. aside and the built-in `advisor()` are consultations, not delegates, so that gate does not cover them (`{{ASIDE_RULE_FILE}}`); dispatch follows its own policy (`{{DISPATCH_RULE_FILE}}`).
